# 📱 iPhone セットアップ手順書 — 食事カロリー記録

iPhoneで外出先でも使えるように、ホーム画面にアイコンを追加する手順です。

---

## 🌐 配信方法（2 通り）

### 方法 A: GitHub Pages で公開（推奨・既存のジム記録ツールと同じ流れ）

ジム記録ツール (`フィットネス記録ツール/`) と同じ手順で、このフォルダも GitHub にアップロードすれば、`https://<ユーザー名>.github.io/<リポジトリ名>/` で iPhone から開けます。

1. このフォルダ（`食事カロリー記録ツール/`）の中で `git init` → GitHub に push
2. リポジトリの Settings → Pages → Branch を `main` / `/ (root)` に設定
3. 1〜2 分後に公開 URL が iPhone Safari から開けるようになります

### 方法 B: Mac でローカル確認のみ

`起動.command` をダブルクリックすると Mac の既定ブラウザで開きます。ただし iPhone から見るには上記の方法 A が必要です。

---

## 📲 iPhone セットアップ（公開 URL を開いた後・5 ステップ）

### ステップ 1: Safari で URL を開く

iPhone の **Safari**（Chrome ではなく Safari）で公開 URL を開きます。
> 📝 一度開いたら、ブックマークしておくと次回が楽です。

### ステップ 2: ホーム画面に追加

1. 画面下中央の **共有ボタン**（□に↑のアイコン）をタップ
2. メニューを下にスクロール → 「**ホーム画面に追加**」をタップ
3. 名前は「食事カロリー」のまま「**追加**」をタップ
4. ホーム画面に「食事カロリー」アイコンが追加されます

### ステップ 3: アプリとして起動

ホーム画面の「食事カロリー」アイコンをタップ → 全画面でアプリのように起動します（アドレスバーなし）。

### ステップ 4: 自分の情報を登録

1. **設定**タブを開く
2. 体重 / 身長 / 年齢 / 性別 / 活動レベルを入力 → 「保存」
3. 計算結果に **基礎代謝・TDEE・目標カロリー** が表示されます
4. 必要に応じて「目標カロリーの手動設定」（ダイエット目的なら TDEE − 300〜500 など）

### ステップ 5: 試しに 1 件記録してみる

1. **今日**タブで「朝食に食品を追加」をタップ
2. お気に入り（★）から「ご飯」などを選択 → 数量「1」 → 追加
3. 朝食欄に追加され、合計カロリーと目標差分が更新されます

---

## 🍱 使い方ガイド

### 4 つの食事区分

| 区分 | アイコン |
|------|---------|
| 朝食 | 🌅 |
| 昼食 | ☀️ |
| 夕食 | 🌙 |
| おやつ | 🍬 |

各区分に食品を複数追加できます。食品ごとに **数量** を指定（例: ご飯 1.5杯、卵 2個）。

### お気に入り（★）

- 「食品」タブで各食品の ★ をタップ → お気に入りに追加
- 食事追加モーダルでは、デフォルトで **お気に入りのみ表示**
- よく食べる食品に ★ をつけておくと、毎日の入力が爆速になります

### 食品の追加（カスタム食品）

- 「食品」タブ → 右上の「＋ 追加」
- 名前 / アイコン（80種類のパレットから選択 or 直接入力）/ kcal / 単位を入力
- 「お気に入りに追加」も同時に設定可能
- 自分が追加した食品は「(追加)」表記、削除も可能

### 履歴

- 「履歴」タブに記録のある日が日付降順で並びます
- 日付をタップ → その日の「今日タブ」に移動して編集可能

### データ管理（設定タブ）

- **CSV エクスポート**: 食事記録のみ。日付・食事区分・食品名・数量・kcal の表形式
- **JSON バックアップ**: ユーザー情報 / 食品マスター / 全記録を 1 つの JSON に
- **バックアップから復元**: 機種変更・誤削除時の復旧用
- **全データ削除**: 食品マスターも初期化されます（2 段階確認）

---

## 🔄 Mac へのデータ移行

iPhone と Mac は **別データ** で動きます（自動同期なし）。

### 方法 A: CSV → メール
1. iPhone の「設定」タブ → 「📄 CSV エクスポート」
2. 共有メニューから「メール」で自分宛に送信
3. Mac で受信 → Excel/Numbers で開く

### 方法 B: AirDrop
1. iPhone で CSV または JSON エクスポート → 共有メニューから「AirDrop」で Mac へ
2. Mac の「ダウンロード」フォルダにファイルが届く

### CSV 列構成
| 日付 | 食事 | 食品名 | アイコン | 数量 | 単位 | 単位 kcal | 合計 kcal |
|------|------|-------|---------|------|------|---------|---------|

---

## ⚠️ 注意事項

- **データ保存場所**: iPhone Safari ブラウザ内（localStorage）
  - Safari の「履歴をすべて削除」やサイトデータ削除をするとデータも消えます
  - 月 1 回 JSON バックアップ をエクスポートして iCloud Drive に保存することを推奨
- **iPhone と Mac は別データ**: それぞれの端末に独立して保存されます
- **食品を削除した場合**: 過去の記録には「(削除済み食品)」と表示されます（カロリー集計からは除外）
- 機種変更時: 旧端末で JSON バックアップ → 新端末で「バックアップから復元」

---

## 🆘 トラブルシューティング

| 症状 | 対処 |
|-----|-----|
| ホーム画面アイコンから起動するとアドレスバーが残る | 「Safari で開く」状態。一度削除して、ホーム画面追加から追加し直してください |
| データが消えた | Safari 設定でサイトデータが削除された可能性。月 1 回 JSON バックアップを推奨 |
| ボタンが反応しない | Safari をいったん終了して再起動してください |
| 目標カロリーが「未設定」のまま | 設定タブで体重・身長・年齢を入れて「保存」を押してください |
